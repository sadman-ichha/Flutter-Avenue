import 'package:flutter/material.dart';

class FavroiteScreen extends StatelessWidget {
  const FavroiteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("Favroite Screen"),
      ),
    );
  }
}
